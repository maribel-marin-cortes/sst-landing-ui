# Local (docker-compose del backend levantado en :8095):
VITE_API_URL=http://127.0.0.1:8095/api
# Producción (Vercel): setea esta misma variable en el dashboard del
# proyecto (Settings -> Environment Variables) apuntando al backend real de
# Render, ej: https://sst-landing-api.onrender.com/api
# Se hornea en el build (Vite), así que un cambio acá requiere un redeploy.
